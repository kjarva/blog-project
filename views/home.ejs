<%- include('partials/header'); -%>
   <h1>Home</h1>

<p><%= homeContent %></p>
<div class="blogContent">
      <% blogPosts.forEach(post => { %>
         <div class="item">
         <h1><%= post.title %></h1>
         <p><%= post.content %></p>
      </div>
      <% }); %>
</div>
<%- include('partials/footer'); -%>
